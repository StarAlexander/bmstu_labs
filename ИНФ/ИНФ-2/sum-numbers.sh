#!/bin/bash


if [ $# -ne 2 ]; then
    echo "Пожалуйста, укажите два числа"
    exit 1
fi

if ! [[ "$1" =~ ^-?[0-9]+$ ]] || ! [[ "$2" =~ ^-?[0-9]+$ ]]; then
    echo "Оба аргумента должны быть числами"
    exit 1
fi

sum=$(( $1 + $2 ))
echo "Сумма: $sum"